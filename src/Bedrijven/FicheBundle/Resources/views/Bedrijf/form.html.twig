<form action="{{ path('BedrijvenFicheBundle_bedrijf_create', {  } ) }}" method="post" {{ form_enctype(form) }} class="bedrijven">
    {{ form_widget(form) }}
    <p>
        <input type="submit" value="Submit">
    </p>
</form>